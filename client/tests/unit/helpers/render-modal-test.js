import {
  renderModal
} from 'geojobs/helpers/render-modal';

module('RenderModalHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = renderModal(42);
  ok(result);
});
